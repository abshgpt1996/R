v1=c('A','B','C')
v1
v7=c('A','C')
v7
v1[!v1 %in% v7]
(mymatrix = matrix(1:24,ncol=6,byrow=T))
#(mymatrix = matrix(11:34,nrow=6,byrow=T))
length(11:35)
mymatrix[,1]
mymatrix[2:4,3:4]

mymatrix
colSums(mymatrix)
rowSums(mymatrix)
rowMeans(mymatrix)
colMeans(mymatrix)
?mymatrix
?matrix
